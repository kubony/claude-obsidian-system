---
name: contact-matcher
description: Google CSV 또는 iCloud VCF 연락처와 인물사전 파일을 분석하여 잠재적 매칭을 찾는 스킬. 사용 시점: (1) "연락처 매칭 분석해줘" (2) "인물사전에 연락처 추가할 사람 찾아줘" (3) Google Contacts CSV를 내보낸 후 인물사전과 매칭 확인이 필요할 때 (4) google-contact-sync 전 매칭 후보 확인이 필요할 때
---

# Contact Matcher

Google 연락처(CSV) 또는 iCloud 연락처(VCF)와 인물사전을 분석하여 잠재적 매칭을 찾는 스킬입니다.

## 목적

인물사전에 연락처 정보가 없는 사람들을 대상으로, 연락처 파일에서 매칭 가능한 연락처를 찾아 제안합니다.

## 사용 시점

1. "연락처 매칭 분석해줘"
2. "인물사전에 연락처 추가할 사람 찾아줘"
3. Google Contacts CSV를 내보낸 후 인물사전과 매칭하고 싶을 때
4. 수동으로 연락처를 추가하기 전에 자동 매칭 후보를 확인하고 싶을 때

## 연락처 파일 위치

```
/Users/inkeun/projects/obsidian/.docs/contacts-google-kubony@gmail.com_20260104.csv
```

## 실행 방법

```bash
source /Users/inkeun/projects/obsidian/.venv/bin/activate && \
python /Users/inkeun/projects/obsidian/.claude/skills/contact-matcher/scripts/analyze_contact_matching.py \
  --contact-file "/Users/inkeun/projects/obsidian/.docs/contacts-google-kubony@gmail.com_20260104.csv" \
  --person-dir "/Users/inkeun/projects/obsidian/04_Networking/00_인물사전" \
  --min-score 7
```

## 파라미터

| 파라미터 | 필수 | 기본값 | 설명 |
|---------|------|--------|------|
| `--contact-file` | 필수 | - | Google CSV 또는 iCloud VCF 파일 경로 |
| `--person-dir` | 필수 | - | 인물사전 디렉토리 경로 |
| `--min-score` | 선택 | 7 | 최소 매칭 점수 (1-25) |
| `--limit` | 선택 | 30 | 출력할 최대 결과 수 |

## 매칭 알고리즘

### 점수 체계

| 조건 | 점수 | 설명 |
|------|------|------|
| 이름 정확 일치 | 10 | 연락처 이름과 파일명 완전 일치 |
| 이름 부분 일치 | 7 | 이름이 서로 포함됨 |
| 성 일치 | 2 | 성만 일치 (2글자 이상) |
| 소속 정확 일치 | 8 | 회사/조직명 완전 일치 |
| 소속 부분 일치 | 5 | 소속이 서로 포함됨 |
| 이메일 일치 | 15 | 이메일 주소 정확 일치 |
| 이메일 도메인 일치 | 3 | 도메인만 일치 |
| 전화번호 일치 | 15 | 전화번호 정확 일치 |

### 필터링

- 연락처 이름에서 직위(팀장, 대리, 이사 등) 제거
- 연락처 이름에서 날짜(YYYYMMDD) 제거
- 점수 7점 이상만 신뢰도 높은 매칭으로 간주

## 출력 예시

```
신뢰도 높은 매칭 후보: 15건

📁 김창환_리얼월드.md
   이름: 김창환, 소속: 리얼월드

   1. 매칭 점수: 18점
      이름: 김창환
      소속: 리얼월드
      전화: 010-1234-5678
      이메일: changwhan@realworld.com
      이유: 이름 정확 일치, 소속 정확 일치

   2. 매칭 점수: 10점
      이름: 김창환
      소속: (없음)
      전화: 010-9876-5432
      이메일: (없음)
      이유: 이름 정확 일치
```

## 후속 작업

매칭 결과를 확인한 후:
1. 신뢰도 높은 매칭 → `google-contact-sync` 스킬로 자동 업데이트
2. 애매한 매칭 → 수동으로 인물사전 파일 편집하여 연락처 추가

## 의존성

- Python 3.8+
- PyYAML: `pip install pyyaml`
- 가상환경: `/Users/inkeun/projects/obsidian/.venv`

## 제한사항

1. **이름 정규화**: 한글 NFD/NFC 정규화 처리 (macOS 호환)
2. **소속 없음**: 파일명에 소속이 없으면 매칭 점수 낮음
3. **중복 이름**: 동명이인은 소속으로 구분 필요
4. **지원 형식**: Google Contacts CSV 또는 iCloud VCF 형식 지원

## 스킬 위치

```
.claude/skills/contact-matcher/
├── SKILL.md (이 파일)
└── scripts/
    └── analyze_contact_matching.py
```
